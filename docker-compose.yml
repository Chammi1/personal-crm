services:
  crm:
    build: .
    container_name: personal-crm
    restart: unless-stopped
    env_file: .env
    volumes:
      - ./data:/app/data
    networks:
      - proxy

networks:
  proxy:
    external: true

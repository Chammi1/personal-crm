-- Бессрочных обещаний больше не бывает: без явной даты ставится мягкий срок
-- +14 дней. Флаг отличает автоматический срок от назначенного вручную,
-- чтобы интерфейс честно показывал «авто».
ALTER TABLE task ADD COLUMN due_auto INTEGER NOT NULL DEFAULT 0;
